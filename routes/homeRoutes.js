//router.get("/", (req,res)=> {
    //Reading from the database
    //Serialize the data after! .get(plain: true)

    res.render("viewName", {
        sendData we want to send to handlebars
    })


})